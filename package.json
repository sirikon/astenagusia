{
  "scripts": {
    "start": "cd src/web && pitch serve --host 0.0.0.0",
    "build": "cd src/web && pitch build",
    "test": "npm run test:common",
    "test:common": "cd src/common && mocha",
    "lint": "eslint .",
    "lint:fix": "npm run lint -- --fix"
  },
  "dependencies": {
    "pitch-cli": "~0.9.0",
    "slugify": "^1.3.1",
    "jquery": "3.4.1",
    "jsdom": "15.1.1"
  },
  "devDependencies": {
    "crawler": "^1.1.4",
    "eslint": "^6.0.1",
    "fuzzy-compare": "^1.0.0",
    "mocha": "^5.2.0"
  }
}
